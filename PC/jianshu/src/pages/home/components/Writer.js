import React from 'react';
import {
    WriterWrapper
} from '../style';

class Writer extends React.PureComponent {
    render() {
        return (
            <WriterWrapper>
                HomeWork
            </WriterWrapper>
        )
    }
}

export default Writer;